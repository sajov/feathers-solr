const { addId, jsonQuery, patchQuery, deleteQuery, suggestQuery, whitelist, defaultOperators } = require('./query');
const { responseFind, responseGet } = require('./response');

module.exports = {
  addId,
  jsonQuery,
  patchQuery,
  deleteQuery,
  suggestQuery,
  whitelist,
  defaultOperators,
  addId,
  responseFind,
  responseGet
};
