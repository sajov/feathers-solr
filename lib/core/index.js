const {
  addId,
  jsonQuery,
  patchQuery,
  deleteQuery,
  suggestQuery,
  defaultOperators
} = require('./query');
const { responseFind, responseGet } = require('./response');

module.exports = {
  addId,
  jsonQuery,
  patchQuery,
  deleteQuery,
  suggestQuery,
  defaultOperators,
  addId,
  responseFind,
  responseGet
};
